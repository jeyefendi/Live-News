const fetchNews =async (
    category?: Category | string,
    keywords?: string,
    isDynamic?: boolean
) => {
    // GraphQL query

    // Fetch func with NEXT 13 caching...

    // Sort func by images vs not images present

    //return res
}